spring:
  application:
    name: scct-gateway-server
  # 指定环境
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:28848
        namespace: dev
        group: DEFAULT_GROUP













